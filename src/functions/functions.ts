/**
 * Adds two numbers.
 * @customfunction TESTSUM
 * @param first First number
 * @param second Second number
 * @returns The sum of the two numbers.
 */
export function testSum(first: number, second: number): number {
  return first + second;
}
